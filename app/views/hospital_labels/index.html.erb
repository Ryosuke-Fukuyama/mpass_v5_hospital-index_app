<h3><%= t('view.hospital_label') + t('view.index') %></h3>

<div class="search_area navbar navbar-light bg-light">
  <%= search_form_for @q, url: hospital_labels_path do |f| %>
    <%= f.label :name_cont, t('view.hospital_label') + t('view.name_of') + ":" %>
    <%= f.search_field :name_cont, placeholder: t('view.placeholder.part') %>

    <%= f.submit class: "btn btn-outline-success btn-sm" %>
  <% end %>
</div>

<div id="hospital_labels_index_table", class="slender_table">
  <%= render "hospital_labels/index_table", hospital_labels: @hospital_labels %>
</div>

<%= paginate @hospital_labels %>

<%= render "hospital_labels/new", hospital_label: @hospital_label %>